import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Mouse clicking interaction, pointing, hover action, pointer
 * @category Actions
 * @keywords mouse, cursor, gesture, interface
 * @variants outlined,filled
 */
declare const CursorArrowClicking: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default CursorArrowClicking;
