import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Analyze chart, dashboard, data visualization
 * @category Actions
 * @keywords analysis, details
 * @variants outlined,filled
 */
declare const ChartColumnMagnifier: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default ChartColumnMagnifier;
