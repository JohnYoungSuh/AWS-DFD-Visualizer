import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Device with two antennas, mobile phone wireless terminal router
 * @category Objects
 * @keywords public network, infrastructure
 * @variants outlined,filled
 */
declare const CellularGateway: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default CellularGateway;
