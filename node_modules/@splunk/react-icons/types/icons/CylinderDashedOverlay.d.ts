import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Copy, clone, or duplicate datasets or storage, as in a Kubernetes StatefulSet
 * @category Data Type
 * @keywords data storage, backup, redundant
 * @variants outlined,filled
 */
declare const CylinderDashedOverlay: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default CylinderDashedOverlay;
