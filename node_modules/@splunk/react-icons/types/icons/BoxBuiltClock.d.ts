import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Scheduled or recurring tasks, such as a Kubernetes CronJob
 * @category Data Flow
 * @keywords workload, packets, clock, time
 * @variants outlined,filled
 */
declare const BoxBuiltClock: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default BoxBuiltClock;
