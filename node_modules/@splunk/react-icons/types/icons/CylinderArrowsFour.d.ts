import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Represents EBS, a cloud-based block storage solution
 * @category Data Flow
 * @keywords aws, s3, data storage, lake
 * @variants outlined,filled
 */
declare const CylinderArrowsFour: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default CylinderArrowsFour;
