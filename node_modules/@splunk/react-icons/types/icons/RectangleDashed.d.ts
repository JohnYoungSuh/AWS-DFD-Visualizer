import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Area or grouping, such as a Kubernetes Namespace for dividing cluster resources
 * @category Actions
 * @keywords outline, category, space
 * @variants outlined,filled
 */
declare const RectangleDashed: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default RectangleDashed;
