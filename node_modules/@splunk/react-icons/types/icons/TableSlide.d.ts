import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Lookup Data Table is a type of dataset for additional data for ICX
 * @category Data Type
 * @keywords dataset
 * @variants outlined,filled
 */
declare const TableSlide: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default TableSlide;
