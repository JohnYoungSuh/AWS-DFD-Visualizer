import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Workload icon, depicting tasks as boxes
 * @category Objects
 * @keywords vehicle, shipping, transport, road, tire, bandwidth
 * @variants outlined,filled
 */
declare const TruckBoxes: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default TruckBoxes;
