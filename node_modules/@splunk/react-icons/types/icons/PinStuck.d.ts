import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Pinned status item, click to unpin in o11y, Move an item on the top of list in Enterprise Security (ES)
 * @category Actions
 * @keywords favorite, task, todo, stick
 * @variants outlined,filled
 */
declare const PinStuck: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default PinStuck;
