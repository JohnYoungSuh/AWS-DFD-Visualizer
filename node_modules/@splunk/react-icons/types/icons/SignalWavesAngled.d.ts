import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Sensor device, sensing technology, microphone, temperature, etc.
 * @category Actions
 * @keywords network, entity, security, signal, wave
 * @variants outlined,filled
 */
declare const SignalWavesAngled: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default SignalWavesAngled;
