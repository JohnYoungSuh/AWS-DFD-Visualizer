import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Sandbox, used for an action to open sandbox in SUI library.
 * @category Objects
 * @keywords closed box, building blocks
 * @variants outlined,filled
 */
declare const Cube: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default Cube;
