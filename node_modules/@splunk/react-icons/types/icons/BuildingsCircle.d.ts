import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Company or organization account
 * @category Data Type
 * @keywords license, permission, corporate, log in, sign
 * @variants outlined,filled
 */
declare const BuildingsCircle: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default BuildingsCircle;
