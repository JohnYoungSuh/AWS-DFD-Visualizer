import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Charging status, lightning bolt represents electrical power, energy status, or high-priority alert indicator
 * @category Objects
 * @keywords alert, quick, power, electricity
 * @variants outlined,filled
 */
declare const Electricity: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default Electricity;
