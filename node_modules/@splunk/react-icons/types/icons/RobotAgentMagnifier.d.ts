import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Investigation, investigating, detective
 * @category Objects
 * @keywords secret service, zoom, spot
 * @variants outlined,filled
 */
declare const RobotAgentMagnifier: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default RobotAgentMagnifier;
