import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Teleworker gateway
 * @category Objects
 * @keywords remote, wireless, drive, network
 * @variants outlined,filled
 */
declare const DeviceSignalWaves: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default DeviceSignalWaves;
