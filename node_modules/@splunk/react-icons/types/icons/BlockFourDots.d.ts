import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description (This icon is found in Observability icon library)
 * @category Objects
 * @keywords block, cube, 3D, components
 * @variants outlined,filled
 */
declare const BlockFourDots: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default BlockFourDots;
