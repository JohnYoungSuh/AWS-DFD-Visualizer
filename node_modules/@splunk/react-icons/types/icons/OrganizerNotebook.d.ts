import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Document organizer notebook, multiple documents, manuals, series
 * @category Objects
 * @keywords documents, file, collection
 * @variants outlined,filled
 */
declare const OrganizerNotebook: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default OrganizerNotebook;
