import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description CCTV, Security camera device, network camera
 * @category Objects
 * @keywords lan, record, video, lens, eye sight
 * @variants outlined,filled
 */
declare const SecurityCamera: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default SecurityCamera;
