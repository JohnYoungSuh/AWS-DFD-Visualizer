import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Application package, deliverable, archive
 * @category Objects
 * @keywords gift box, close, open, delivery
 * @variants outlined,filled
 */
declare const BoxCardboard: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default BoxCardboard;
