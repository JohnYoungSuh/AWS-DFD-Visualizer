import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Indexer cluster manager in Splunk Enterprise
 * @category Data Flow
 * @keywords master node, index, search head
 * @variants outlined,filled
 */
declare const DriveSliders: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default DriveSliders;
