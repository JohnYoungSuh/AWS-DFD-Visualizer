import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Design works, draft, proposal, planning
 * @category Objects
 * @keywords scale, ruler, building, measurement, metrics
 * @variants outlined,filled
 */
declare const TriangleRuler: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default TriangleRuler;
