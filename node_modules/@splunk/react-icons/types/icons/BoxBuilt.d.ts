import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Stacked or queued jobs, representing multiple tasks in a Kubernetes environment
 * @category Data Flow
 * @keywords workload, packets
 * @variants outlined,filled
 */
declare const BoxBuilt: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default BoxBuilt;
