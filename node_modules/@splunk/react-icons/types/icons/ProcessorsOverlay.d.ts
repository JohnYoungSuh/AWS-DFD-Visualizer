import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Represents EC2, which provides scalable virtual servers in the cloud
 * @category Data Flow
 * @keywords machine, hardware management
 * @variants outlined,filled
 */
declare const ProcessorsOverlay: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default ProcessorsOverlay;
