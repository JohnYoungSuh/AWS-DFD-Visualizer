import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Filtering and sorting in descending
 * @category Actions
 * @keywords search results, table, alphabetical
 * @variants outlined,filled
 */
declare const FilterArrowUp: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default FilterArrowUp;
