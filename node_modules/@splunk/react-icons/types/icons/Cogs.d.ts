import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Settings and advanced configuration; the Kubernetes cluster icon signifies a collection of machines
 * @category Navigation
 * @keywords gears, multiple configurations
 * @variants outlined,filled
 */
declare const Cogs: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default Cogs;
