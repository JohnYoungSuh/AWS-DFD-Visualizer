import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Time Duration indicates processing or waiting status
 * @category Actions
 * @keywords loading, processing, timer
 * @variants outlined,filled
 */
declare const HourglassCircle: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default HourglassCircle;
