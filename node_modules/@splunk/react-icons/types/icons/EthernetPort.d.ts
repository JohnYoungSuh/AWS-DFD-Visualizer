import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Ethernet cable port, network switching hub
 * @category Objects
 * @keywords gigabit, local, physical, cat6
 * @variants outlined,filled
 */
declare const EthernetPort: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default EthernetPort;
