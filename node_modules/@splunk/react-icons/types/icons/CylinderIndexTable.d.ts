import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Data storage for Index data source, Splunk core
 * @category Data Type
 * @keywords event database, source type
 * @variants outlined,filled
 */
declare const CylinderIndexTable: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default CylinderIndexTable;
