import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description API icon
 * @category Data Type
 * @keywords code, xml, configuration, text
 * @variants outlined,filled
 */
declare const CogChevronsSlash: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default CogChevronsSlash;
