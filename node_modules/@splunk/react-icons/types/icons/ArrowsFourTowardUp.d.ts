import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined';
}
/**
 * @description Wireless LAN controller
 * @category Controls
 * @keywords router, wi-fi, local network
 * @variants outlined
 */
declare const ArrowsFourTowardUp: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default ArrowsFourTowardUp;
