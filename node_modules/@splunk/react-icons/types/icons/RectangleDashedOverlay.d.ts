import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Copy, clone, or duplicate objects or groups, as with a Kubernetes ReplicaSet
 * @category Actions
 * @keywords outline, fade, delete, update
 * @variants outlined,filled
 */
declare const RectangleDashedOverlay: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default RectangleDashedOverlay;
