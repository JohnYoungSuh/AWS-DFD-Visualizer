import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Finger tapping interaction, pointing
 * @category Actions
 * @keywords hand, cursor, gesture, interface
 * @variants outlined,filled
 */
declare const FingerPointClicking: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default FingerPointClicking;
