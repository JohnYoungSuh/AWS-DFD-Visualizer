import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description User identification, passport, drivers license, security
 * @category Workspace
 * @keywords identify, member, account, personal
 * @variants outlined,filled
 */
declare const IdentityCard: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default IdentityCard;
