import React from 'react';
import SVG from '@splunk/react-icons/SVG';
export interface IconBaseProps extends React.ComponentProps<typeof SVG> {
    children?: React.ReactNode;
    /**
     *  Defaults to outlined, determines the variant of the icon to be rendered
     */
    variant?: 'default' | 'outlined' | 'filled';
}
/**
 * @description Service or package configuration, symbolized by a Kubernetes Node
 * @category Actions
 * @keywords box, entity, configurations, gear, product
 * @variants outlined,filled
 */
declare const CubeCog: ({ children, variant, ...otherprops }: IconBaseProps) => React.JSX.Element | null;
export default CubeCog;
