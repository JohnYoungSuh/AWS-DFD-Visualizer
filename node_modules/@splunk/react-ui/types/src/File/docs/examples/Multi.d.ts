import React, { Component } from 'react';
import { FileRequestAddHandler, FileRequestRemoveHandler } from '@splunk/react-ui/File';
interface FileItem {
    name: string;
    value?: string | ArrayBuffer | null;
}
/**
 * @name Multifile
 * @description By storing an array of items, multiple files can be supported.
 */
declare class Multi extends Component<object, {
    files: FileItem[];
}> {
    static loadFile(file: globalThis.File): FileItem;
    constructor(props: object);
    handleAddFiles: FileRequestAddHandler;
    handleRemoveFile: FileRequestRemoveHandler;
    render(): React.JSX.Element;
}
export default Multi;
