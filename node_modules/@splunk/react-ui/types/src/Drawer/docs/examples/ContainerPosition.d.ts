import React from 'react';
/**
 * @name ContainerPosition
 * @description The `container` position renders the drawer relative to its nearest positioned ancestor.
 */
declare function ContainerPosition(): React.JSX.Element;
export default ContainerPosition;
