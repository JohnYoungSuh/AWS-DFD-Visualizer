import React from 'react';
/**
 * @name PagePosition
 * @description The `page` position (default) renders the drawer relative to the viewport using a portal.
 */
declare function PagePosition(): React.JSX.Element;
export default PagePosition;
