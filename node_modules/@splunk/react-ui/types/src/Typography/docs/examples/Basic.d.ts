import React from 'react';
/**
 * @name Basic
 * @description Style text with a variant, font settings props, or both. Typography will default to the body variant if no other TypographyParams props are passed (an as prop is always required).
 */
declare function Basic(): React.JSX.Element;
export default Basic;
