import React from 'react';
import { ComponentProps } from '../utils/types';
type ItemFocusHandler = (event: React.FocusEvent<HTMLButtonElement>, data: {
    itemKey?: number;
}) => void;
interface ItemPropsBase {
    /**
     * Active shows a temporarily selected state, similar to that of focus. This is used when filtering the
     * `Menu` items in Multiselect, Select, and ComboBox and when navigating with arrows.
     */
    active?: boolean;
    /** Becomes the label. Must be a string if using `matchRanges`. */
    children?: React.ReactNode;
    /**
     * Additional information to explain the option.
     */
    description?: string;
    /**
     * The description text might appear to the right of the label or under the label.
     */
    descriptionPosition?: 'right' | 'bottom';
    /**
     * Prevents user interaction and adds disabled styling.
     * If set to `dimmed`, the component is able to be focused, and is more easily located.
     * If set to `disabled`, the disabled attribute is set which makes the component unable to be focused and more difficult to locate.
     * The default behavior when `disabled={true}` is `dimmed`.
     */
    disabled?: boolean | 'dimmed' | 'disabled';
    /**
     * A React ref which is set to the DOM element when the component mounts and null when it unmounts.
     */
    elementRef?: React.Ref<HTMLAnchorElement | HTMLButtonElement>;
    /** Adornment after the label. */
    endAdornment?: React.ReactNode;
    /**
     * Adds an icon to the right to show that there is a submenu. To implement submenus, use the `SlidingPanels` component.
     */
    hasSubmenu?: boolean;
    /**
     * @private. The internal key of the item
     */
    itemKey?: number;
    /**
     * Sections of the label string to highlight as a match.
     */
    matchRanges?: {
        start: number;
        end: number;
    }[];
    /**
     * Callback for click events.
     */
    onClick?: React.MouseEventHandler<HTMLAnchorElement | HTMLButtonElement>;
    onFocus?: ItemFocusHandler;
    /**
     * Open the "to" link in a new context, which is usually a new tab or window based on browser settings.
     *
     * An icon and a screen reader message is added to indicate this behavior to users.
     * The default message is "(Opens new window)"; this can be customized by passing
     * a string instead of boolean to `openInNewContext`.
     */
    openInNewContext?: boolean | string;
    /**
     * @private
     * Prevents the element from becoming focused. Used to keep the focus inside the filter box
     * in <Combobox>, <Select>, and <Multiselect.Compact>
     */
    preventFocus?: boolean;
    /**
     * The default role is inferred from the other props. `selectable` with a
     * `selectableAppearance` of 'checkmark' defaults to `menuitemradio`. `selectable` with a
     * `selectableAppearance` of 'checkbox' defaults to `menuitemcheckbox`. Otherwise, the role defaults
     * to `menuitem`.
     */
    role?: 'menuitem' | 'menuitemradio' | 'menuitemcheckbox' | 'listboxitem' | 'link' | 'option';
    /**
     * Enables selection for this item and reserves space for the control.
     * Required when using `selected` or `selectableAppearance`.
     */
    selectable?: boolean;
    /**
     * Specifies the type of selection control to display when `selectable` is enabled.
     * Use `'checkmark'` for single-select menus and `'checkbox'` for multi-select menus.
     */
    selectableAppearance?: 'checkmark' | 'checkbox';
    /**
     * Controls the selection state of the item.
     * The selection control (checkmark or checkbox) is only rendered when `selectable` is enabled.
     *
     * When `selectableAppearance = 'checkmark'`:
     * - `true` shows a checkmark.
     *
     * When `selectableAppearance = 'checkbox'`:
     * - `true` renders a checked box.
     * - `'some'` renders an indeterminate box (`aria-checked="mixed"`).
     * - `false` renders an unchecked box.
     */
    selected?: boolean | 'some';
    /** Adornment in front of the label. */
    startAdornment?: React.ReactNode;
    /**
     * The URL or path to link to.
     */
    to?: string;
    /**
     * When `true`, wrapping is disabled and any additional text is truncated using an ellipsis. A `Tooltip` will render so users can see the full text on hover.
     */
    truncate?: boolean;
}
type SharedItemProps = Omit<ItemPropsBase, 'selectable' | 'selectableAppearance' | 'selected'>;
interface NonSelectableItemProps extends SharedItemProps {
    selectable?: false;
    selectableAppearance?: never;
    selected?: never;
}
interface SelectableItemProps extends SharedItemProps {
    selectable: true;
    selectableAppearance?: 'checkmark' | 'checkbox';
    selected?: boolean | 'some';
}
type ItemPropsCommon = NonSelectableItemProps | SelectableItemProps;
type ItemButtonProps = ComponentProps<ItemPropsCommon, 'button'>;
type ItemLinkProps = ComponentProps<ItemPropsCommon, 'a', 'href'>;
type ItemProps = ItemButtonProps | ItemLinkProps;
declare function Item({ active, children, description, descriptionPosition, disabled, endAdornment, elementRef, hasSubmenu, itemKey, matchRanges, onClick, onFocus, openInNewContext, role, selectable, selectableAppearance, selected, startAdornment, to, truncate, ...otherProps }: ItemProps): React.JSX.Element;
declare namespace Item {
    var propTypes: React.WeakValidationMap<ItemProps>;
    var as: string;
}
export default Item;
export { ItemFocusHandler };
export type { ItemPropsBase };
